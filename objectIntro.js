/**
 * Created by ah on 2017/9/10.
 */
// When a function is called as a method—looked up as a property and immediately called, as in object.method()—
// the special variable "this" in its body will point to the object that it was called on.
//物件中的method裏的this 變數會指向物件本體

